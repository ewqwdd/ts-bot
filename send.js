// const { sendMessage } = require("./messages");
// const User = require("./User");

// const sendAll = async (cb) => {
//   const users = await User.find();
//   users.forEach(elem => {
//       cb(elem.telegramId, sendMessage)
//   })
// };

// module.exports = { sendAll };
